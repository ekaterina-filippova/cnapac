// Use http://localhost:8080/pipeline-syntax/ to generate job steps

node('master') {
    def version
    stage('Build') {
        //sh 'make build'
    }
    stage('Test') {
        //sh 'make test'
    }
    stage('Image') {
        //sh 'make image'
    }
    stage('Push') {
        //sh 'make push'
    }
    stage('Run e2e tests') {
        // TODO: run another job and pass a parameter
    }
}